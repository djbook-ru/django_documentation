To create .pot files run:

$ sphinx-build -b gettext . _build/translation

To create .po file

$ msginit -i _build/translation/index.pot -o locale/ru/LC_MESSAGES/index.po -l ru

To create .mo file run:

$ msgfmt locale/ru/LC_MESSAGES/index.po -o locale/ru/LC_MESSAGES/index.mo

Create doc:

$ sphinx-build -E -b djangohtml -d _build/doctrees   . _build/html